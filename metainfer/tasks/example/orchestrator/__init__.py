"""Orchestrator plugin registration.

In a real task, this file is imported by ``metainfer/tasks/X/__init__.py``
and calls ``register(PLUGIN)`` so the launcher knows which CLI module to
invoke for this task type.
"""

# --- Uncomment and customise for a real task ---

# from metainfer.orchestrator.tasks import register
# from .plugin import PLUGIN
# register(PLUGIN)
