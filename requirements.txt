# MetaInfer runtime dependencies — pure Python, no compiled extensions.
# Install with: pip install -r requirements.txt
#
# These mirror [project].dependencies in pyproject.toml; this file is
# provided as a convenience for users who don't want to `pip install -e .`
# and just want to run ./serve.py from a fresh checkout.

fastapi>=0.110
uvicorn>=0.27
pyyaml>=6.0
pydantic>=2.0      # comes transitively via fastapi, pinned for safety
